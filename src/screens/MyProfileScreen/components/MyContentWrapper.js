import styled from "styled-components";
import { colors } from "../../../constants";

export const MyContentWrapper = styled.View`
  flex: 1;
  align-items: center;
  padding-bottom: 10;
  background-color: ${colors.background};
`;

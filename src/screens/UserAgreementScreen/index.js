import { connect } from "react-redux";
import UserAgreementScreenComponent from "./UserAgreementScreen";

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export const UserAgreementScreen = connect(
  mapStateToProps,
  mapDispatchToProps
)(UserAgreementScreenComponent);

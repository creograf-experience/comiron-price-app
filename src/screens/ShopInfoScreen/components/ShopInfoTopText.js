import styled from "styled-components";
import { verticalScale } from "react-native-size-matters";

export const ShopInfoTopText = styled.Text`
  font-size: 16;
  padding-bottom: ${verticalScale(5)};
`;

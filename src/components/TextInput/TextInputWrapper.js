import styled from "styled-components";
import { scale } from "react-native-size-matters";

export const TextInputWrapper = styled.View`
  height: ${scale(60)};
  padding-vertical: ${scale(2.5)};
  justify-content: space-between;
`;

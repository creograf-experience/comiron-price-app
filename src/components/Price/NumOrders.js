import styled from "styled-components";
import { colors } from "../../constants";

export const NumOrders = styled.View`
  width: 25;
  height: 25;
  border-radius: 15;
  align-items: center;
  justify-content: center;
  background-color: ${colors.inStockColor};
`;
